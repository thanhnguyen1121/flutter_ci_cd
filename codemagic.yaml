# Automatically generated on 2023-01-30 UTC from https://codemagic.io/app/63d7730398da2dfc54008da3/settings
# Note that this configuration is not an exact match to UI settings. Review and adjust as necessary.

workflows:

  default-workflow:
    name: Default Workflow
    max_build_duration: 60
    environment:
      vars: # Define your public environment variables here
        API_KEY: "86ec2d09-dfc2-4822-95aa-7e90430aa802"
        DISTRIBUTION_KEY: "d27d805b7d3efd33a9b2ed4cee72bf4e92ce8e1a"
        USER_API_HOST: "https://deploygate.com/organizations/thanhnguyen9908/platforms/android/apps"

#        https://deploygate.com/distributions/d27d805b7d3efd33a9b2ed4cee72bf4e92ce8e1a

      flutter: default
      xcode: latest
      cocoapods: default
      groups:
        - firebase_credentials
    triggering:
      events:
        - tag
      branch_patterns: # Include or exclude watched branches
        - pattern: 'develop'
          include: true
          source: true
      tag_patterns:
        - pattern: 'app-dev-v*'
          include: true
        - pattern: excluded-tag
          include: false
        - pattern: included-tag
          include: true

    scripts:
      - |
        # set up debug keystore
        rm -f ~/.android/debug.keystore
        keytool -genkeypair \
          -alias androiddebugkey \
          -keypass android \
          -keystore ~/.android/debug.keystore \
          -storepass android \
          -dname 'CN=Android Debug,O=Android,C=US' \
          -keyalg 'RSA' \
          -keysize 2048 \
          -validity 10000
      - |
        # set up local properties
        echo "flutter.sdk=$HOME/programs/flutter" > "$FCI_BUILD_DIR/android/local.properties"
      - flutter packages pub get
      - flutter build apk --release --no-shrink
#      - find . -name "Podfile" -execdir pod install \;
#      - flutter build ios --debug --no-codesign
#      - curl -sSL https://deploygate.com/cli/install/f03d58a1 | bash
      - |
        Curl -H "Authorization: token $API_KEY" -F "file=@build/app/outputs/flutter-apk/app-release.apk" \ 
        -F "message=New distribution" \
        -F "distribution_key=$DISTRIBUTION_KEY" \
        -F "release_note=Release Note" "$USER_API_HOST"
#      - dg login --terminal --email 'thanhnguyen9908@gmail.com' --password 'Anhthanh1102'
#      - dg deploy build/app/outputs/flutter-apk/app-release.apk --message 'sample demo'
#        --user 'thanhnguyen9908'
#        --distribution-key '86ec2d09-dfc2-4822-95aa-7e90430aa802'
#      - dg deploy /Users/builder/clone/build/ios/iphoneos/Runner.app --message 'sample demo'
#        --user 'thanhnguyen9908'
#        --distribution-key '86ec2d09-dfc2-4822-95aa-7e90430aa802'

    artifacts:
      - build/**/outputs/apk/**/*.apk
      - build/**/outputs/bundle/**/*.aab
      - build/**/outputs/**/mapping.txt
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - build/windows/**/*.msix
      - flutter_drive.log
    publishing:
      email:
        recipients:
          - thanhnguyen9908@gmail.com
#      firebase:
#        firebase_service_account: $FIREBASE_SERVICE_ACCOUNT
#        android:
#          app_id: 1:347194776072:android:35fff4068f09f9201e8fbd
#          groups:
#            - thanhnguyen
#
#          artifact_type: 'apk'

